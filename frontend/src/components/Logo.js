import React from 'react'
import trangdevLogo from '../assets/logo.png';

const Logo = ({w, h}) => {
    return (
        <img src={trangdevLogo} alt="Logo" width={w} height={h}/>
    )
}
export default Logo
